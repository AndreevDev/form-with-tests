import React from "react";
import { ControlWrapper } from "../StyledComponents";

const SubmitButton = () => {

    return (
        <ControlWrapper justifyContent="center">
            <input type="submit"/>
        </ControlWrapper>
    );
};

export default SubmitButton;